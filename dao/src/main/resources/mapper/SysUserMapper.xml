<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zac.flycloud.mapper.SysUserMapper">

    <!-- 根据用户名查询 -->
    <select id="getUserByName" resultType="com.zac.flycloud.entity.tablemodel.SysUser">
        select *
        from sys_user
        where username = #{username}
          and enable_status = 1
    </select>

    <!-- 根据手机号查询 -->
    <select id="getUserByPhone" resultType="com.zac.flycloud.entity.tablemodel.SysUser">
        select *
        from sys_user
        where phone = #{phone}
          and enable_status = 1
    </select>

    <!-- 根据邮箱查询用户信息 -->
    <select id="getUserByEmail" resultType="com.zac.flycloud.entity.tablemodel.SysUser">
        select *
        from sys_user
        where email = #{email}
          and enable_status = 1
    </select>

    <!-- 查询用户的所属部门名称信息 -->
    <select id="getDeptsByUserUuid" resultType="com.zac.flycloud.entity.tablemodel.SysDept">
        select *
        from sys_dept
        where uuid in
        (select dept_uuid from sys_user_dept where user_uuid = #{userUuid})
    </select>

    <!-- 查询用户的角色信息 -->
    <select id="getRolesByUserUuid" resultType="com.zac.flycloud.entity.tablemodel.SysRole">
        select *
        from sys_role
        where uuid in
              (select role_uuid from sys_user_role where user_uuid = #{userUuid})
    </select>

</mapper>